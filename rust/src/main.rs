#[allow(unused_imports)]
use std::{
    cmp::min,
    collections::{HashMap, VecDeque},
    io::{self, BufRead},
};

struct Scheduler {}

impl Scheduler {
    fn new() -> Self {
        todo!()
    }

    fn new_job(&mut self, id: usize, steps: impl Iterator) {
        todo!()
    }

    fn kill_job(&mut self, id: usize) {
        todo!()
    }

    fn tick(&mut self) -> usize {
        todo!()
    }
}

fn main() {}
